# Быстрый старт - ME Shop System

## 1. Подготовка оборудования

### Вам понадобится:
- **OpenComputers Компьютер**:
  - Computer Case (любой уровень)
  - CPU (T1 или выше)
  - Memory (RAM) - минимум 2x T1
  - Hard Drive - минимум 1MB
  - Screen + Keyboard
  - Graphics Card (опционально, для лучшего отображения)

- **Applied Energistics 2**:
  - ME Interface или ME Controller
  - ME Drives с дисками
  - Энергия для ME сети

- **Для связи**:
  - Adapter (OpenComputers)

## 2. Установка

### Шаг 1: Физическое подключение
```
[ME Controller] ---- [ME Cable] ---- [ME Interface]
                                            |
                                       [Adapter]
                                            |
                                       [Computer]
```

Или проще:
```
[Computer] → [Adapter] → [ME Interface]
```

### Шаг 2: Проверка подключения
1. Включите компьютер
2. Запустите команду:
```lua
lua
components = require("component")
for name, addr in components.list() do print(name, addr) end
```

3. Вы должны увидеть один из компонентов:
   - `me_interface`
   - `me_controller`
   - `me_exportbus`
   - `me_importbus`

### Шаг 3: Установка программ

**Вариант A: С Internet Card**
```bash
pastebin get XXXXX install
install
```

**Вариант B: Ручная установка**
1. Скопируйте все `.lua` файлы на дискету или HDD
2. Скопируйте их в `/home/`

### Шаг 4: Первый запуск
```bash
lua test_me_api.lua
```

Это проверит подключение к ME системе.

## 3. Использование

### Главная программа - me_shop.lua
Запуск:
```bash
lua me_shop.lua
```

Или создайте алиас:
```bash
edit /home/.shrc
```

Добавьте строку:
```bash
alias shop="lua /home/me_shop.lua"
```

Теперь можно запускать просто:
```bash
shop
```

### Быстрый поиск
```bash
lua price_search.lua алмаз
lua price_search.lua diamond
lua price_search.lua блок
```

## 4. Настройка цен на предметы

### Как добавить цену к предмету:

1. **Через лор предмета** (рекомендуется для серверов):
   Используйте команду (нужны права):
   ```
   /give @p minecraft:diamond 1 0 {display:{Lore:["Минимальная цена: 2.25$"]}}
   ```

2. **Через переименование** (если поддерживается):
   В наковальне переименуйте предмет, добавив цену:
   ```
   Алмаз - 2.25$
   ```

3. **Поддерживаемые форматы**:
   - `Минимальная цена: 2.25$`
   - `цена: 2.25$`
   - `Price: 2.25$`
   - `2.25$`
   - `2.25руб`
   - `2.25₽`

### Примеры команд для установки цены:

**Алмазный блок:**
```
/give @p minecraft:diamond_block 1 0 {display:{Name:"Алмазный блок (#0057)",Lore:["minecraft:diamond_block","Минимальная цена: 2.25$","Доступен для обмена в трейдере на другие предметы.","Minecraft"]}}
```

**Золото:**
```
/give @p minecraft:gold_ingot 1 0 {display:{Lore:["цена: 1.50$"]}}
```

**Изумруд:**
```
/give @p minecraft:emerald 1 0 {display:{Lore:["Price: 3.00$"]}}
```

## 5. Проблемы и решения

### "ME компонент не найден"
- Проверьте, что Adapter подключен к ME Interface
- Убедитесь, что кабель правильно подключен
- Проверьте, что ME сеть получает энергию
- Перезагрузите компьютер: `reboot`

### "Не удалось получить список предметов"
- ME система должна быть полностью загружена
- Проверьте энергию в сети
- Убедитесь, что в ME есть хотя бы один предмет
- Попробуйте команду напрямую:
  ```lua
  lua
  me = require("component").me_interface
  items = me.getItemsInNetwork()
  print(#items)
  ```

### Цены не отображаются
- Убедитесь, что формат цены правильный
- Проверьте детальную информацию о предмете:
  ```bash
  lua me_shop.lua
  # Выберите "4. Детальная информация"
  ```
- При необходимости добавьте свой формат в [config.lua](config.lua)

### Программа работает медленно
- Уменьшите количество проверок в мониторе
- Используйте фильтры для уменьшения количества обрабатываемых предметов
- Установите более мощный CPU и больше RAM

## 6. Полезные команды

### Экспорт всего инвентаря
```bash
lua export_prices.lua inventory.txt
```

### Мониторинг каждые 30 секунд
```bash
lua price_monitor.lua 30
```

### Поиск всех предметов с "diamond"
```bash
lua price_search.lua diamond
```

## 7. Расширенная настройка

### Изменение форматов цен
Редактируйте [config.lua](config.lua):
```bash
edit /home/config.lua
```

Добавьте свой паттерн в `config.pricePatterns`:
```lua
config.pricePatterns = {
    "Минимальная цена:%s*([%d%.]+)%$",
    "Ваш формат:%s*([%d%.]+)евро",  -- Добавьте здесь
}
```

### Автозапуск при загрузке
Создайте файл `.shrc` в `/home/`:
```bash
edit /home/.shrc
```

Добавьте:
```bash
# Автозапуск мониторинга цен
lua /home/price_monitor.lua 60 &
```

## 8. Дополнительная информация

- **Официальная документация OpenComputers**: https://ocdoc.cil.li/
- **API Applied Energistics**: https://ocdoc.cil.li/component:applied_energistics
- **Lua туториалы**: https://www.lua.org/pil/

## 9. Советы и трюки

### Создание ценника для магазина
```lua
-- Создайте программу shop_display.lua
local component = require("component")
local gpu = component.gpu

function displayTopItems()
    local me = component.me_interface
    local items = me.getItemsInNetwork()
    
    gpu.setResolution(80, 25)
    gpu.fill(1, 1, 80, 25, " ")
    
    gpu.set(1, 1, "=== МАГАЗИН - ПОПУЛЯРНЫЕ ТОВАРЫ ===")
    
    -- Отображаем первые 10 предметов с ценами
    local y = 3
    for _, item in ipairs(items) do
        if item.label and item.label:match("%d+%$") then
            gpu.set(1, y, item.label)
            y = y + 1
            if y > 20 then break end
        end
    end
end

while true do
    displayTopItems()
    os.sleep(60)  -- Обновление каждую минуту
end
```

### Интеграция с Redstone
```lua
-- Подключите Redstone I/O к компьютеру
local component = require("component")
local rs = component.redstone

-- Включить сигнал если товаров мало
function checkStock()
    local me = component.me_interface
    local items = me.getItemsInNetwork({label = "Алмаз"})
    
    if #items > 0 and items[1].size < 64 then
        rs.setOutput(0, 15)  -- Включить сигнал
    else
        rs.setOutput(0, 0)   -- Выключить
    end
end
```

---

**Готово!** Теперь у вас есть полнофункциональная система управления ME складом с ценами! 🎉
