# Quick Start - Smart Shop

## Быстрая установка за 5 минут

### 1️⃣ Подключите оборудование

```
ME Controller → Adapter → Computer
                          ↓
                 Transposer #1 → [Сундук приема денег]
                 Transposer #2 → [Сундук выдачи]
```

**Важно:** Transposer'ы положите СВЕРХУ на сундуки!

---

### 2️⃣ Установите OpenOS

```bash
# Вставьте дискету OpenOS, включите компьютер
install
# Следуйте инструкциям, затем перезагрузитесь
reboot
```

---

### 3️⃣ Скопируйте код

```bash
edit /home/shop.lua
# Вставьте код из shop.lua (правый клик мыши)
# Ctrl+S - сохранить, Ctrl+W - выйти
```

---

### 4️⃣ Запустите!

```bash
./shop.lua
```

---

## ⚡ Первый тест

### Что проверить сразу:

1. **Все компоненты найдены?**
   - ✓ ME Controller
   - ✓ 2 Transposer'а
   - ✓ GPU

2. **Нажмите `R`** - загрузятся предметы из ME

3. **Смотрите логи:**
   - Если видите "💰 ЦЕНА НАЙДЕНА" - **отлично!** Парсинг работает
   - Если все "⚠ Цена не найдена" - нужен конфиг (см. ниже)

---

## 🔧 Если парсинг НЕ работает

### Вариант А: Быстрый конфиг

```bash
edit /home/config.lua
```

Вставьте:
```lua
return {
    getPrice = function(itemName, damage)
        local prices = {
            ["minecraft:diamond"] = 100.0,
            ["minecraft:iron_ingot"] = 5.0,
            -- Добавьте свои предметы
        }
        return prices[itemName] or 10.0
    end
}
```

Затем измените shop.lua (в начале):
```lua
local hasConfig, config = pcall(require, "config")
```

И в функции `getItemsFromME()` после `getDetailedItemInfo(item)`:
```lua
if hasConfig and not itemInfo.price then
    itemInfo.price = config.getPrice(item.name, item.damage)
end
```

---

## 🎮 Управление

| Клавиша | Действие |
|---------|----------|
| `↑↓` | Выбрать предмет |
| `Enter` | Купить |
| `D` | Пополнить баланс |
| `R` | Обновить список |
| `Esc` | Выход |
| `Любая буква` | Поиск |
| `Backspace` | Удалить символ |

---

## 💡 Первая покупка

1. **Пополните баланс:**
   - Нажмите `D`
   - Положите деньги в сундук
   - Нажмите `Enter`
   - Проверьте баланс на экране

2. **Найдите товар:**
   - Начните вводить название
   - Используйте ↑↓ для выбора

3. **Купите:**
   - Нажмите `Enter`
   - Заберите из сундука выдачи

---

## ❓ Проблемы?

### "ME Controller не найден"
→ Adapter должен быть вплотную к ME Interface

### "Transposers не найдены"
→ Transposer'ы должны лежать СВЕРХУ на сундуках

### "Деньги не переносятся"
→ Проверьте `moneyItem = "contenttweaker:money"` в config
→ Убедитесь что ME система принимает этот предмет

### "Крафт не работает"
→ Добавьте паттерны в ME Interface
→ Или предметы будут экспортироваться напрямую

---

## 📚 Подробнее

- [README.md](README.md) - полная документация
- [TESTING_GUIDE.md](TESTING_GUIDE.md) - детальное тестирование
- [EXAMPLES.md](EXAMPLES.md) - примеры вывода
- [test-config.lua](test-config.lua) - пример конфига с ценами

---

## 🚀 Готово!

Система должна работать. Если парсинг цен работает - вы увидите:

```
✓ Парсинг цен работает! (X%)
```

Если нет - используйте конфиг файл.

**Удачных продаж!** 💰
