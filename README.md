# OpenComputers Smart Shop - Инструкция по установке

> 🚀 **Быстрый старт:** См. [QUICKSTART.md](QUICKSTART.md) для установки за 5 минут!
>
> 🧪 **Тестирование:** См. [TESTING_GUIDE.md](TESTING_GUIDE.md) для детального тестирования парсинга цен
>
> 📊 **Примеры:** См. [EXAMPLES.md](EXAMPLES.md) для примеров вывода системы

## ✨ Новые возможности

### 🔍 Умный парсинг цены (4 метода)
Система автоматически пытается извлечь цену из описания предмета:
1. **"Минимальная цена: 15.0$"** - русский формат
2. **"Цена: 15.0$"** или **"Price: 15.0$"** - короткий формат
3. **"15.0$"** или **"$15.0"** - просто число с символом
4. **"15.0"** - любое дробное число

### 🎯 Умный поиск с приоритетами
- Точное совпадение → Начинается с → Содержит → По словам
- Работает с русским и английским
- Показывает количество найденных результатов

### 📊 Детальная отладка
- Анализ NBT данных каждого предмета
- Показывает содержимое Lore (описания)
- Статистика успешности парсинга
- Цветные индикаторы (✓ - цена найдена, ? - дефолт)

### 🎨 Улучшенный GUI
- Визуальные индикаторы распознанных цен
- Цветовая дифференциация
- Счетчик результатов поиска
- Подсказки по интерфейсу

---

## Необходимые компоненты

### 1. Компьютер OpenComputers

Минимальная конфигурация:
- **Case (Tier 2+)** - корпус компьютера
- **CPU (Tier 2+)** - процессор
- **Memory (2x Tier 2)** - минимум 2 планки памяти
- **Hard Drive (Tier 2+)** - жесткий диск
- **Graphics Card (Tier 2+)** - видеокарта
- **Screen (Tier 2+, 80x25)** - экран
- **Keyboard** - клавиатура
- **EEPROM (Lua BIOS)** - загрузчик
- **OpenOS Floppy** - для установки (затем можно вынуть)

### 2. Периферия

#### Applied Energistics 2:
- **ME Controller** - контроллер ME сети
- **ME Interface** - интерфейс для подключения
- **ME Storage** - хранилище предметов
- **Molecular Assembler** - для автокрафта (опционально)
- **Patterns** - паттерны крафта для нужных предметов

#### OpenComputers:
- **Adapter (2 штуки)** - адаптеры для подключения
- **Transposer (2 штуки)** - для работы с сундуками
- **Cable** - кабели для связи

#### Minecraft:
- **Chest (2 штуки)** - обычные сундуки

---

## Схема подключения

```
                    [ME Controller]
                          |
                     [ME Interface]
                          |
                      [Adapter]
                          |
┌─────────────────────────┼─────────────────────────┐
│                    [Computer]                      │
│                         │                          │
│            ┌────────────┴────────────┐             │
│            │                         │             │
│      [Transposer 1]           [Transposer 2]       │
│            │                         │             │
│      [Chest: Деньги]          [Chest: Выдача]     │
└────────────────────────────────────────────────────┘
```

### Подробное подключение:

1. **ME Controller → Adapter #1**
   - Поставьте Adapter вплотную к ME Interface/Controller
   - Этот адаптер позволит компьютеру видеть ME систему

2. **Transposer #1 (Деньги)**
   - Положите Transposer НА сундук для денег (сверху)
   - Подключите к компьютеру кабелем
   - Или подключите непосредственно к компьютеру снизу

3. **Transposer #2 (Выдача)**
   - Положите Transposer НА сундук выдачи (сверху)
   - Подключите к компьютеру кабелем

4. **Computer**
   - Подключите экран, клавиатуру
   - Соедините кабелями с Adapter и Transposer'ами
   - Вставьте все компоненты в Case

---

## Установка ПО

### Шаг 1: Установка OpenOS

1. Вставьте **OpenOS Floppy** в компьютер
2. Включите компьютер
3. Дождитесь загрузки и выполните:
   ```bash
   install
   ```
4. Следуйте инструкциям установщика
5. После установки выньте дискету и перезагрузитесь:
   ```bash
   reboot
   ```

### Шаг 2: Загрузка скрипта

Два способа:

#### Способ А: Через wget (если есть Internet Card)
```bash
wget https://raw.githubusercontent.com/YOUR_REPO/shop.lua /home/shop.lua
```

#### Способ Б: Ручное копирование
1. Скопируйте содержимое файла `shop.lua`
2. В OpenComputers выполните:
   ```bash
   edit /home/shop.lua
   ```
3. Вставьте код (правый клик мыши или средняя кнопка)
4. Сохраните: `Ctrl+S`, выйдите: `Ctrl+W`

### Шаг 3: Настройка автозапуска (опционально)

Чтобы магазин запускался автоматически:
```bash
edit /home/.shrc
```

Добавьте в конец файла:
```bash
/home/shop.lua
```

Сохраните и перезагрузитесь.

---

## Настройка ME системы

### 1. Паттерны крафта

Для каждого предмета, который должен крафтиться:
1. Создайте паттерн в **Pattern Terminal**
2. Положите паттерн в **ME Interface** рядом с **Molecular Assembler**
3. Убедитесь, что в ME есть все ресурсы для крафта

### 2. Проверка доступа

В компьютере выполните:
```bash
lua
```
```lua
component = require("component")
me = component.me_controller
print(#me.getItemsInNetwork())
```

Должно показать количество уникальных предметов в ME.

---

## Настройка сундуков

### Сундук #1: Прием денег
- Должен быть доступен для всех игроков
- Transposer лежит СВЕРХУ на сундуке
- Игроки кладут `contenttweaker:money` в этот сундук

### Сундук #2: Выдача товаров
- Может быть с защитой от добавления предметов
- Transposer лежит СВЕРХУ
- Из этого сундука игроки забирают купленное

---

## Использование системы

### Для покупателя:

1. **Пополнение баланса:**
   - Нажмите `D` в меню
   - Положите деньги (`contenttweaker:money`) в сундук приема
   - Нажмите `Enter`
   - Баланс пополнится автоматически

2. **Поиск товара:**
   - Начните вводить название предмета
   - Используйте `↑↓` для выбора

3. **Покупка:**
   - Выберите предмет
   - Нажмите `Enter`
   - Заберите товар из сундука выдачи

### Горячие клавиши:
- `D` - пополнить баланс
- `R` - обновить список товаров
- `↑↓` - навигация
- `Enter` - купить
- `Backspace` - удалить символ в поиске
- `ESC` - выход

---

## Отладка и диагностика

### Проблема: "ME Controller не найден"

**Решение:**
1. Проверьте, что Adapter вплотную к ME Interface
2. Убедитесь, что ME система включена (контроллер горит)
3. В компьютере выполните:
   ```bash
   components
   ```
   Должен быть `me_controller`

### Проблема: "Transposers не найдены"

**Решение:**
1. Transposer должен лежать НА сундуке (сверху)
2. Проверьте подключение кабелями к компьютеру
3. Выполните `components` - должно быть минимум 2 `transposer`

### Проблема: "Деньги не переносятся"

**Решение:**
1. Проверьте, что в скрипте правильно указан `moneyItem = "contenttweaker:money"`
2. Убедитесь, что Transposer смотрит вниз на сундук
3. Проверьте, что ME система может принимать этот предмет

### Проблема: "Товар не крафтится"

**Решение:**
1. Убедитесь, что в ME Interface есть паттерн крафта
2. Проверьте наличие ресурсов в ME
3. Добавьте Molecular Assembler к интерфейсу

### Проблема: "Цена не парсится"

**Важно:** Парсинг цены из описания предмета может не работать на всех серверах!

**Симптомы:**
- В логах: "Lore пуст или недоступен"
- "⚠ Цена не найдена, используем дефолт: 10.0$"
- В итогах: "Цены найдены: 0"
- Все предметы отмечены символом "?"

**Как проверить:**
1. Запустите магазин
2. Нажмите `R` для загрузки предметов
3. Смотрите в терминал - там будет детальный анализ
4. Если видите "💰 ЦЕНА НАЙДЕНА" хотя бы раз - парсинг работает!
5. Если все "⚠ Цена не найдена" - парсинг НЕ работает

**Решение - используйте конфиг файл:**

1. Создайте `/home/config.lua`:
```lua
return {
    getPrice = function(itemName, damage)
        local prices = {
            ["minecraft:diamond"] = 100.0,
            ["minecraft:emerald"] = 50.0,
            ["minecraft:iron_ingot"] = 5.0,
            -- Добавьте свои предметы и цены
        }
        return prices[itemName] or 10.0
    end
}
```

2. В `shop.lua` добавьте после `local unicode = require("unicode")`:
```lua
local hasConfig, config = pcall(require, "config")
if hasConfig then
    debug("✓ Конфигурационный файл загружен", "SUCCESS")
end
```

3. В функции `getItemsFromME()` после строки `local itemInfo = getDetailedItemInfo(item)` добавьте:
```lua
-- Если цена не найдена, пробуем конфиг
if hasConfig and not itemInfo.price then
    itemInfo.price = config.getPrice(item.name, item.damage)
    debug("  Цена из конфига: " .. itemInfo.price .. "$", "INFO")
end
```

**Альтернатива:** См. [test-config.lua](test-config.lua) для полного примера конфига

---

### Проблема: "Парсинг работает частично"

**Симптом:** Некоторые предметы с ценой, некоторые без

**Причина:** Формат описания отличается у разных предметов

**Решение:**
1. Посмотрите в логах строки Lore проблемных предметов
2. Добавьте свой паттерн в функцию `parsePrice()`:
```lua
-- Попытка 5: Ваш формат
for _, line in ipairs(lore) do
    local price = string.match(line, "Ваш шаблон: ([%d%.]+)")
    if price then
        return tonumber(price)
    end
end
```

---

## Расширенная настройка

### Изменение валюты

В файле `shop.lua` измените:
```lua
moneyItem = "minecraft:emerald",  -- ID предмета
moneyName = "Изумруды",           -- Отображаемое имя
```

### Изменение цветов

```lua
colors = {
    bg = 0x000000,        -- Фон
    header = 0x4B4B4B,    -- Шапка
    primary = 0x2196F3,   -- Основной (голубой)
    success = 0x4CAF50,   -- Успех (зеленый)
    error = 0xF44336,     -- Ошибка (красный)
    text = 0xFFFFFF,      -- Текст (белый)
}
```

### Изменение сторон подключения

Если ваша конфигурация отличается, в скрипте измените `sides`:
- `sides.up` - верх
- `sides.down` - низ
- `sides.north` - север
- `sides.south` - юг
- `sides.east` - восток
- `sides.west` - запад

---

## Безопасность

### Рекомендации:

1. **Защитите компьютер:**
   ```bash
   chmod 600 /home/shop.lua
   ```

2. **Установите пароль на BIOS** (через EEPROM)

3. **Используйте Claim Block** для защиты территории

4. **Регулярно проверяйте логи** (debug сообщения)

---

## FAQ

**Q: Можно ли использовать несколько магазинов?**
A: Да, но каждый должен иметь свой компьютер и ME систему.

**Q: Работает ли с другими модами хранения?**
A: Нет, только Applied Energistics 2.

**Q: Можно ли продавать предметы игрокам?**
A: Да, потребуется дополнительная доработка скрипта.

**Q: Как добавить лимиты покупок?**
A: Нужно добавить систему логирования и ограничений.

---

## Поддержка

Для отладки смотрите сообщения в терминале компьютера.
Все действия логируются с цветовой индикацией:
- **БЕЛЫЙ** - информация
- **ЗЕЛЕНЫЙ** - успех
- **КРАСНЫЙ** - ошибка
- **ЖЕЛТЫЙ** - предупреждение
