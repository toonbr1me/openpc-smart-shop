# Быстрый старт с NBT парсингом

## Что изменилось?

✅ **Парсинг цен из Lore теперь РАБОТАЕТ!**

Вместо ручного config.lua система может автоматически читать цены из описания предметов.

## Установка (5 минут)

### 1. Дополнительные компоненты

Добавьте к существующей системе:
- **Database Upgrade (Tier 3)** - установить в Adapter

### 2. Загрузка файлов

Скопируйте на компьютер OpenComputers:
```bash
cd /home
wget https://your-server/price-parser.lua price-parser.lua
wget https://your-server/test-nbt-via-database.lua test-nbt-via-database.lua
```

Или скопируйте вручную через `edit` команду.

### 3. Тестирование

```bash
# Проверьте доступность NBT данных
./test-nbt-via-database.lua
```

**Ожидаемый вывод:**
```
=== Тест получения NBT через Database ===

✓ ME Controller найден
✓ Database найден (адрес: 12ab34cd)

Получаем список предметов из ME...
Найдено предметов в ME: 156

Тестируем первые 3 предмета:

─────────────────────────────────────
Предмет #1: Diamond
  ID: minecraft:diamond
  Damage: 0
  Количество: 64
  hasTag: true

  Попытка сохранить в Database слот 1...
  ✓ Сохранено в Database!

  Данные из Database.get(1):
  ──────────────────────────
    name: minecraft:diamond
    label: Diamond
    damage: 0
    tag: <table>

  Проверка NBT структуры:
    ✓ tag существует
    ✓ tag.display существует
    ✓ tag.display.Lore найден!

    LORE СОДЕРЖИМОЕ:
      [1] §7Минимальная цена: 100.0$
      [2] §9Редкий ресурс

  Hash предмета: a1b2c3d4e5f6...
```

### 4. Запуск магазина

```bash
./shop.lua
```

**Вывод при старте с NBT:**
```
✓ Модуль парсинга цен из NBT загружен
✓ Конфигурационный файл с ценами загружен
Инициализация компонентов...
✓ ME Controller найден: 12ab34cd
✓ Database найден: 56ef78gh
  → Парсинг цен из Lore ДОСТУПЕН
✓ Transposers настроены
...
Обработано 156 предметов
✓ Спарсено из Lore: 142 цен
✓ Загружено из config.lua: 14 цен
```

## Режимы работы

### 1. Только NBT парсинг (рекомендуется)

**Требуется:**
- Database Upgrade
- price-parser.lua

**Преимущества:**
- ✅ Автоматическое обновление цен
- ✅ Не нужен config.lua
- ✅ Цены берутся с сервера

**Недостатки:**
- ⚠️ Небольшая задержка при загрузке (~0.05с на 10 предметов)
- ⚠️ Цены должны быть в Lore предметов на сервере

### 2. Гибридный режим (лучше всего)

**Требуется:**
- Database Upgrade
- price-parser.lua
- config.lua

**Преимущества:**
- ✅ NBT парсинг для предметов с Lore
- ✅ config.lua fallback для остальных
- ✅ Максимальная гибкость

**Пример config.lua:**
```lua
return {
    -- Цены только для предметов БЕЗ Lore
    priceList = {
        ["contenttweaker:special_item:0"] = 50.0,
        ["minecraft:dirt:0"] = 0.1
    },
    
    -- Категориальные дефолты
    categoryPrices = {
        ["ore"] = 5.0,
        ["ingot"] = 10.0
    },
    
    getPrice = function(itemName, damage)
        local key = itemName .. ":" .. damage
        if priceList[key] then return priceList[key] end
        
        for category, price in pairs(categoryPrices) do
            if itemName:find(category) then
                return price
            end
        end
        
        return 1.0  -- дефолт
    end
}
```

### 3. Только config.lua (старый способ)

**Требуется:**
- config.lua

**Используйте если:**
- ❌ Нет Database Upgrade
- ❌ Предметы не имеют Lore
- ✅ Нужен полный контроль цен

## Формат цен в Lore

Ваш сервер должен добавлять цены в Lore предметов в одном из форматов:

```
§7Минимальная цена: 15.0$
§7Цена: 15.0$
§7Price: 15.0$
§7Стоимость: 15.0$
§715.0$
§715$
```

**Цвет не важен** (§7, §a, §f и т.д.)

## Производительность

### Начальная загрузка:
- **100 предметов** → ~0.5 секунд
- **500 предметов** → ~2.5 секунд
- **1000 предметов** → ~5 секунд

### Использование памяти:
- Database хранит **только 1 предмет** одновременно
- Автоматическая очистка после чтения
- Циклическое использование 81 слота

### CPU нагрузка:
- Минимальная
- Парсинг только для `hasTag=true` предметов
- Остальные используют кеш

## Troubleshooting

### "Database компонент не найден!"

**Решение:**
1. Установите Database Upgrade в Adapter
2. Перезагрузите компьютер: `reboot`
3. Проверьте: `lua` → `=component.list("database")()`

### "tag.display.Lore отсутствует"

**Причина:** Предмет не имеет описания на сервере

**Решение:**
- Добавьте цены в config.lua для таких предметов
- Или попросите администратора добавить Lore к предметам

### "Ни config.lua, ни price-parser.lua не найдены!"

**Решение:**
- Создайте хотя бы один файл:
  - `price-parser.lua` для NBT парсинга
  - `config.lua` для ручных цен

### Медленная загрузка

**Нормально:** 0.05с на 10 предметов с NBT

**Если медленнее:**
1. Проверьте энергию ME системы
2. Уменьшите количество предметов в сети (временно)
3. Используйте config.lua для часто покупаемых

## Миграция со старой версии

### Если у вас уже есть config.lua:

1. Добавьте Database Upgrade
2. Скопируйте price-parser.lua
3. Запустите shop.lua
4. **Система автоматически** будет:
   - Парсить из NBT если доступно
   - Fallback на config.lua

### Если хотите только NBT:

1. Удалите/переименуйте config.lua
2. Убедитесь что Database установлен
3. Запустите shop.lua

## FAQ

**Q: Нужно ли обновлять config.lua при изменении цен на сервере?**
A: Нет! NBT парсинг берёт актуальные цены автоматически.

**Q: Можно ли использовать без Database?**
A: Да, но тогда нужен config.lua с ценами.

**Q: Сколько стоит Database Upgrade?**
A: Tier 3 = 2x Tier 2 + RAM + чип и т.д. (см. рецепты мода)

**Q: Парсится ли NBT для всех предметов?**
A: Только для `hasTag=true`. Остальные используют config/дефолт.

**Q: Можно ли кешировать NBT цены?**
A: Пока нет, но цены загружаются только при старте магазина.

## Следующие шаги

✅ Протестируйте `./test-nbt-via-database.lua`
✅ Запустите магазин
✅ Посмотрите логи парсинга
✅ Создайте config.lua fallback (опционально)

📖 **Детали:** [NBT_PARSING_SOLUTION.md](NBT_PARSING_SOLUTION.md)
