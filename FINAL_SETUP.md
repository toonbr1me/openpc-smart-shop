# ⚠️ ВАЖНО: Парсинг цен НЕВОЗМОЖЕН

## Вывод после тестирования

По официальной документации OpenComputers + Applied Energistics 2:

**`getItemsInNetwork()` НЕ ВОЗВРАЩАЕТ NBT данные!**

### Что это значит:

❌ **Невозможно** получить `tag.display.Lore` (описание предмета)  
❌ **Невозможно** парсить цену из описания  
❌ **Невозможно** читать любые NBT теги через ME API  

✅ **Возможно** получить: `name`, `label`, `size`, `damage`, `maxSize`, `hasTag` (bool)  
✅ **hasTag** показывает ЧТО теги есть, НО не дает к ним доступа  

---

## Решение: Обязательный config.lua

Система **ТРЕБУЕТ** файл `/home/config.lua` с ценами:

### Создайте config.lua:

```lua
-- Конфигурация цен для Smart Shop
local prices = {
    ["minecraft:diamond"] = 100.0,
    ["minecraft:emerald"] = 50.0,
    ["minecraft:iron_ingot"] = 5.0,
    ["minecraft:gold_ingot"] = 10.0,
    -- Добавьте все ваши предметы
}

-- Категории для быстрого назначения
local categories = {
    ["ore"] = 10.0,
    ["ingot"] = 5.0,
    ["gem"] = 50.0,
}

return {
    getPrice = function(itemName, damage)
        -- Точное совпадение с damage
        local fullName = damage > 0 and (itemName .. ":" .. damage) or itemName
        if prices[fullName] then
            return prices[fullName]
        end
        
        -- Без damage
        if prices[itemName] then
            return prices[itemName]
        end
        
        -- Поиск по категории
        local lower = itemName:lower()
        for category, price in pairs(categories) do
            if lower:find(category) then
                return price
            end
        end
        
        -- Дефолт
        return 10.0
    end
}
```

---

## Быстрый старт

### 1. Создайте config.lua

```bash
edit /home/config.lua
# Вставьте конфиг сверху
# Ctrl+S, Ctrl+W
```

### 2. Скопируйте shop.lua

```bash
edit /home/shop.lua
# Вставьте код
# Ctrl+S, Ctrl+W
```

### 3. Запустите

```bash
./shop.lua
```

Система автоматически загрузит цены из config.lua!

---

## Как узнать ID предметов?

### Способ 1: F3 + H в игре
1. Нажмите F3 + H
2. Наведите на предмет
3. Появится ID типа `minecraft:diamond`

### Способ 2: Через OpenComputers

```bash
lua
```

```lua
component = require("component")
me = component.me_controller
items = me.getItemsInNetwork()

for i, item in ipairs(items) do
    print(item.name .. " - " .. item.label)
end
```

Скопируйте ID и добавьте в config.lua

---

## Структура файлов

```
/home/
├── config.lua       # ОБЯЗАТЕЛЬНО! Цены предметов
├── shop.lua         # Основной скрипт
└── simple-test.lua  # Тест ME системы (опционально)
```

---

## Тестирование

### Проверка ME системы:

```bash
./simple-test.lua
```

Покажет:
- ✓ Работает ли ME система
- ✓ Сколько предметов
- ✓ Доступен ли крафт
- ⚠ Подтверждение что NBT недоступен

---

## FAQ

### Q: Почему нельзя парсить цены?
**A:** Ограничение ME API. `getItemsInNetwork()` не возвращает NBT данные.

### Q: Можно ли это обойти?
**A:** Нет. Это архитектурное ограничение интеграции AE2 + OC.

### Q: Что делать?
**A:** Использовать config.lua - это единственный способ.

### Q: Как быстро заполнить config.lua?
**A:** 
1. Используйте категории (`ore`, `ingot`, `gem`)
2. Добавляйте цены по мере необходимости
3. Дефолтная цена 10$ для неизвестных предметов

### Q: Можно ли добавлять цены в процессе?
**A:** Да! Просто отредактируйте config.lua и перезапустите магазин.

---

## Преимущества config.lua

✅ **Полный контроль** над ценами  
✅ **Быстрая настройка** через категории  
✅ **Легко редактировать** - обычный Lua файл  
✅ **Не зависит** от NBT/описания предметов  
✅ **Работает** на ЛЮБОЙ версии AE2/OC  

---

## Итоговая настройка

1. ✅ Подключите компоненты (см. README.md)
2. ✅ Создайте config.lua с ценами
3. ✅ Скопируйте shop.lua
4. ✅ Запустите `./shop.lua`
5. ✅ Готово!

---

**Удачной торговли!** 💰🛒
