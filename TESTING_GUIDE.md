# Руководство по тестированию Smart Shop

## Что добавлено в новой версии

### 🔍 Умный парсинг цены

Система теперь пытается найти цену в описании предмета 4 способами:

1. **"Минимальная цена: 15.0$"** - русский формат с ключевым словом
2. **"Цена: 15.0$"** или **"Price: 15.0$"** - короткий формат
3. **"15.0$"** или **"$15.0"** - просто число с символом доллара
4. **"15.0"** или **"15.50"** - любое дробное число в строке

### 🎯 Умный поиск с приоритетами

Теперь поиск работает с приоритетами:

1. **Точное совпадение** - "diamond" найдет точно "Diamond" в первую очередь
2. **Начинается с** - "dia" найдет "Diamond", "Diamond Block" и т.д.
3. **Содержит** - "mond" найдет "Diamond"
4. **По словам** - "iron ingot" найдет "Iron Ingot"

### 📊 Детальная отладка

Система показывает подробную информацию о каждом предмете:
- Наличие NBT данных
- Содержимое Lore (описания)
- Результаты парсинга цены
- Статистику (сколько цен найдено/не найдено)

### 🎨 Визуальные индикаторы

В интерфейсе:
- **✓** - цена успешно распознана из описания
- **?** - используется дефолтная цена 10$
- Зеленый цвет - распознанная цена
- Серый цвет - дефолтная цена

---

## Пошаговое тестирование

### Шаг 1: Подготовка сервера

1. Убедитесь что у вас есть предметы с описанием в ME системе
2. Проверьте формат описания на предмете (F3 + H для детального режима)
3. Если описание в формате "Минимальная цена: 15.0$" - отлично!
4. Если нет - система покажет это в логах

### Шаг 2: Установка и запуск

```bash
# На компьютере OpenComputers
cd /home
edit shop.lua
# Вставьте код (правый клик мыши)
# Ctrl+S для сохранения, Ctrl+W для выхода

# Запуск
./shop.lua
```

### Шаг 3: Проверка инициализации

При запуске вы увидите:

```
========================================
    OpenComputers Smart Shop System
========================================

[INFO] Инициализация компонентов...
[SUCCESS] ✓ ME Controller найден: 12345678
[INFO] Найден Transposer: abcd1234
[INFO] Найден Transposer: efgh5678
[SUCCESS] ✓ Transposers настроены
[INFO]   Сундук для денег: abcd1234
[INFO]   Сундук выдачи: efgh5678
[SUCCESS] ✓ GPU настроен (80x25)

✓ Система готова к работе!
```

**Что проверить:**
- Все компоненты найдены (зеленые ✓)
- Нет красных ошибок
- Показаны адреса компонентов

### Шаг 4: Проверка парсинга (КЛЮЧЕВОЙ МОМЕНТ)

Нажмите `R` для загрузки предметов. Вы увидите детальный анализ:

```
=== ДЕТАЛЬНЫЙ АНАЛИЗ ПРЕДМЕТОВ ===

[1/10] Анализ: Алмаз
  ID: minecraft:diamond
  Количество: 64
  Damage: 0
  hasTag: true
  Получение детальной информации о предмете...
    Предмет имеет NBT данные
    Найден tag.display.Lore
    Lore содержит 2 строк:
      [1] Редкий ресурс
      [2] Минимальная цена: 100.0$
  Попытка парсинга цены из lore...
    ✓ Найдено через 'Минимальная цена': 100.0
  💰 ЦЕНА НАЙДЕНА: 100.00$

[2/10] Анализ: Изумруд
  ID: minecraft:emerald
  ...
  ⚠ Цена не найдена, используем дефолт: 10.0$

=== ИТОГИ АНАЛИЗА ===
Всего предметов: 10
Цены найдены: 3
Цены не найдены: 7
✓ Парсинг цен работает! (30%)
```

**Что проверить:**

✅ **ПАРСИНГ РАБОТАЕТ если:**
- Видите "💰 ЦЕНА НАЙДЕНА" хотя бы для одного предмета
- В итогах "Цены найдены: > 0"
- Показывается зеленое "✓ Парсинг цен работает!"

❌ **ПАРСИНГ НЕ РАБОТАЕТ если:**
- Все предметы показывают "⚠ Цена не найдена"
- "Цены найдены: 0"
- Красное "✗ Парсинг цен НЕ работает"
- Lore пуст: "Lore пуст или недоступен"

### Шаг 5: Тестирование поиска

В интерфейсе магазина:

**Тест 1: Точный поиск**
```
Введите: diamond
Ожидается: Diamond, Diamond Ore, Diamond Block в начале списка
```

**Тест 2: Частичный поиск**
```
Введите: dia
Ожидается: Diamond, Diamond Ore на верху
```

**Тест 3: Поиск по содержанию**
```
Введите: mond
Ожидается: Diamond найден
```

**Тест 4: Поиск по словам**
```
Введите: iron ingot
Ожидается: Iron Ingot найден даже если слова не подряд
```

**Что проверить:**
- Счетчик "(найдено: X)" обновляется
- Релевантные результаты появляются выше
- Поиск работает на русском и английском

### Шаг 6: Тестирование пополнения баланса

1. Нажмите `D` в меню
2. Положите деньги (`contenttweaker:money`) в сундук
3. Нажмите `Enter`

**Ожидаемый результат:**
```
[INFO] Проверка денег в сундуке...
[INFO]   Слот 1: 10 денег
[INFO]   Слот 2: 5.5 денег
[INFO] Перенос денег в ME систему...
[SUCCESS]   Перенесено 10 из слота 1
[SUCCESS]   Перенесено 5.5 из слота 2
[SUCCESS] Пополнение баланса на 15.5$

✓ Баланс пополнен на 15.50$
Текущий баланс: 15.50$
```

**Что проверить:**
- Деньги исчезли из сундука
- Баланс увеличился на правильную сумму
- Поддерживаются дробные значения (5.5$)

### Шаг 7: Тестирование покупки

1. Выберите предмет стрелками ↑↓
2. Нажмите `Enter`

**Ожидаемый результат:**
```
[INFO] Крафт предмета: minecraft:diamond x1
[SUCCESS] Найден рецепт крафта!
[INFO] Крафт запущен, ожидание...
[SUCCESS] Крафт завершен!
[SUCCESS] Экспортировано: 1 шт.

✓ Куплено! Заберите из сундука. Баланс: 5.50$
```

**Что проверить:**
- Баланс уменьшился на цену предмета
- Предмет появился в сундуке выдачи
- Крафт выполнен (если предмет требует крафта)

---

## Диагностика проблем

### Проблема: "Lore пуст или недоступен"

**Причина:** ME API на вашем сервере не возвращает NBT данные через `getItemsInNetwork()`

**Решения:**

1. **Используйте конфиг файл:**
   ```bash
   cd /home
   edit config.lua
   # Вставьте содержимое test-config.lua
   ```

2. **Измените shop.lua:**
   Найдите функцию `getDetailedItemInfo()` и добавьте в начало:
   ```lua
   local config = require("config")
   ```
   
   Затем в `getItemsFromME()` замените:
   ```lua
   if not itemInfo.price then
       itemInfo.price = config.getPrice(item.name, item.damage)
   end
   ```

### Проблема: "Цена парсится неправильно"

**Причина:** Формат описания отличается

**Решение:** Посмотрите в логах строки Lore:
```
[1] Редкий ресурс
[2] Минимальная цена: 100.0$
```

Добавьте свой формат в функцию `parsePrice()`:
```lua
-- Ваш формат
local price = string.match(line, "Ваш формат: ([%d%.]+)")
```

### Проблема: "Крафт не работает"

**Причина:** Нет паттернов в ME Interface

**Решение:**
1. Создайте паттерны в Pattern Terminal
2. Положите в ME Interface рядом с Molecular Assembler
3. Убедитесь что есть ресурсы в ME системе

Альтернатива: система попробует прямой экспорт если крафт недоступен

### Проблема: "Деньги не переносятся в ME"

**Причина:** Неправильная сторона Transposer

**Решение:** Измените `sides.down` на нужную:
```lua
-- В функции transferMoneyToME()
local transferred = transposerMoney.transferItem(
    sides.up,    -- Сундук (где лежит Transposer)
    sides.north, -- Измените на вашу сторону ME
    item.size, 
    slot
)
```

Проверьте командой в Lua:
```lua
component = require("component")
sides = require("sides")
t = component.transposer
-- Проверьте все стороны
for k,v in pairs(sides) do print(k, v) end
```

---

## Продвинутое тестирование

### Тест производительности

Загрузите 100+ предметов в ME и проверьте:
- Время загрузки списка
- Скорость поиска
- Отзывчивость интерфейса

### Тест граничных случаев

1. **Пустая ME система** - должна показать пустой список
2. **Балан 0** - не должен позволить купить
3. **Баланс точно равен цене** - должен купить и обнулиться
4. **Предмет без стока** - должен показать ошибку крафта
5. **Дробные цены** - 15.50$, 0.01$, 999.99$

### Тест Unicode

Введите в поиск:
- Русские буквы: "алмаз", "изумруд"
- Специальные символы: "§", "±"
- Emoji (если поддерживаются)

---

## Ожидаемые результаты

### ✅ Успешное тестирование

- Все компоненты инициализированы
- Парсинг работает минимум на 1 предмете
- Поиск находит релевантные результаты
- Баланс пополняется и списывается корректно
- Предметы выдаются в сундук

### ⚠️ Частичный успех

- Компоненты работают
- Парсинг не работает, но конфиг решит проблему
- Все остальное функционирует

### ❌ Провал

- Компоненты не найдены → проверьте подключение
- ME API недоступен → проверьте версию AE2/OpenComputers
- Ошибки Lua → проверьте синтаксис, версию OpenOS

---

## Логирование для разработчика

Добавьте логирование в файл:
```lua
local function debug(message, level)
    -- ... существующий код ...
    
    -- Добавьте запись в файл
    local file = io.open("/home/shop.log", "a")
    if file then
        file:write(os.date("[%Y-%m-%d %H:%M:%S] ") .. "[" .. level .. "] " .. message .. "\n")
        file:close()
    end
end
```

Затем проверяйте:
```bash
tail -f /home/shop.log
```

---

## Следующие шаги

После успешного тестирования:

1. **Настройте цены** - через конфиг или описания
2. **Добавьте автозапуск** - в `/home/.shrc`
3. **Защитите систему** - пароль, claim blocks
4. **Мониторинг** - логи, статистика продаж
5. **Расширение** - скидки, лимиты, мультивалюта

Удачи! 🚀
